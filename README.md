# Document-Based-QA-System
This is an AI that uses vector search to provides answers from within a document. Upon upload, the text is chunked and embedded using OpenAI embeddings. A user query is also embedded, and the most similar chunk(s) are passed to generate an answer with context.
